import { LicensePlate } from './license.plate';

export class Car {
  private readonly brand: string;
  private readonly licensePlate: LicensePlate;
  private readonly vehicleIdentificationNumber: VehicleIdentificationNumber;
}
